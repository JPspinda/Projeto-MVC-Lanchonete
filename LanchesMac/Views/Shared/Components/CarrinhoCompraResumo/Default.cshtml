@model CarrinhoCompraViewModel

@if(Model.CarrinhoCompra.CarrinhoCompraItens.Count > 0)
{
    <form class="d-flex">
        <a class="btn btn-outline-dark" asp-controller="CarrinhoCompra" asp-action="Index">
            <i class="bi-cart-fill me-1"></i>
            <span class="badge bg-dark text-white ms-1 rounded-pill">@Model.CarrinhoCompra.CarrinhoCompraItens.Count</span>
        </a>
    </form>
    @* <c>
        Carrinho <span class="badge-secondary">@Model.CarrinhoCompra.CarrinhoCompraItens.Count</span>
    </a> *@
}