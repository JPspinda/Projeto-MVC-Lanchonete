@model LancheListViewModel;

@* <div>
    <h2>@ViewData["Titulo"]</h2>
</div>
<h2>@ViewData["Data"]</h2>

<hr />
<h3>@ViewBag.Total @ViewBag.TotalLanches</h3>
<hr /> *@

<div>
    <h2>@Model.CategoriaAtual</h2>
</div>

@*<h3>@TempData["Nome"];</h3>*@

<div class="container text-center">
    <div class="row align-items-center">
        @foreach (var lanche in Model.Lanches)
        {
            <partial name="_LanchesResumo" for="@lanche"/> <!--No caso deste for="" estou falando para pegar os dados da variável lanche-->
        }
    </div>
</div>